[tool.poetry]
name = "audit-agent"
version = "0.1.0"
description = "AuditPal reference audit agent"
authors = ["AuditPal"]

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
slither-analyzer = "^0.10.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"